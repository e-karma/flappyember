{{#page-header}}
  <h1 class='pull-left'>
    <span>Previewing {{name}}</span>
  </h1>
{{/page-header}}

{{#page-body}}
  <div class="page-editor">
    {{render-page previewPage}}
  </div>
{{/page-body}}
